# transactions Package
>  transactions package is a compendium of functions used in the `transactions_kedro` pipeline.
## Overview
The functions basically help you transform what client-data could look like, calculating targets for churn, generating many features based on the necessity, the combined process can be used in the `transactions_kedro` pipeline
## Usage

```sh
$  pip  install  segai.transactions
```
