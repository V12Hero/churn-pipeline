# segmentation_core Package
>  Segmentation core is a utility package that helps in the process for data transformation and the use of new kedro datasets, it helps both kedro and python core packages.
## Overview
The package is a compression of datasets type classes and helpers.
## Usage

```sh
$  pip  install  segai.segmentation_core
```
